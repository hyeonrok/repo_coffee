<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jlook.infra.code.CodeDao">

	<resultMap id="resultMapObj" type="com.jlook.infra.code.CodeDto"></resultMap>

	<select id="selectList" resultMap="resultMapObj"> <!-- 애매 -->

         select
            a.codeGroupSeq
             ,a.codeGroupName
             ,b.codeSeq
             ,b.codeName
             ,b.codeMemo
             ,b.codeDelNy
             ,b.codeModDate
         	 ,b.codeRegDate
         	 ,b.codeGroupSeqF
         from 
             codeGroup a
             
         left join  code b
         
         on  a.codeGroupSeq = b.codeGroupSeqF

	   WHERE 1=1
	  
	        <if test="shDelNy != null and shDelNy.equals('')">AND b.delNy = #{shDelNy}</if>
	     
	     <!--
			<choose>
				<when test="shOptionDate == 1">AND a.regDatetime BETWEEN #{shDateStart} AND #{shDateEnd}</when>
				<when test="shOptionDate == 2">AND a.modDatetime BETWEEN #{shDateStart} AND #{shDateEnd}</when>
			</choose>
	-->
	    	 <choose>
	         	 <when test="shOption == 1">AND b.codeSeq = #{shValue}</when>
	            <when test="shOption == 2">AND b.codeName LIKE CONCAT('%',#{shValue},'%')</when>
	       	 </choose>
			
	</select>
	
	<select id="selectOne" resultMap="resultMapObj">
		select
			codeSeq
			,codeName
			,codeMemo
			,codeDelNy
			,codeRegDate
			,codeModDate
			,codeGroupSeqF
		from code 
		where 1 = 1
			AND codeSeq = #{codeSeq}
	</select>
	
	<insert id="insert">
		insert into code (
		codeName
		,codeMemo
		,codeDelNy
		,codeRegDate
		,codeModDate
		,codeGroupSeqF
		) values (
		#{codeName}
		,#{codeMemo}
		,0
		,now()
		,now()
		,#{codeGroupSeqF}
		)
	</insert>
	
	<update id="update">
		update code
		set
		codeName = #{codeName}
		,codeMemo=#{codeMemo}
		,codeModDate=now()
		,codeDelNy=0
		,codeGroupSeqF = #{codeGroupSeqF}
    		where 1=1
    		and codeSeq = #{codeSeq}
	</update>
	
	<update id="uelete">
		update code
		set
		codeDelNy = 1
    		where 1=1
    		and codeSeq = #{codeSeq}
	</update>
	
	<delete id="delete">
		delete from code
		where 1 = 1
			and codeSeq = #{codeSeq}
	</delete>
	  
	  <select id="selectListCachedCodeArrayList" resultMap="resultMapObj">
		SELECT 
			codeSeq
			, codeName
			, codeGroupSeqF
		FROM
			code
		WHERE 1=1
			AND codeDelNy = 0
   </select>
</mapper>